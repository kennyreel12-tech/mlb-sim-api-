{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": { "startCommand": "gunicorn sim_api:app --bind 0.0.0.0:$PORT --timeout 300 --worker-class gthread --threads 4" }
}
